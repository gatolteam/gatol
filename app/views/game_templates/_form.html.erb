<%= form_for(@game_template) do |f| %>
  <% if @game_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_template.errors.count, "error") %> prohibited this game_template from being saved:</h2>

      <ul>
      <% @game_template.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
